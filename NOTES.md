* built on top of console.* API
  * so you can use log.js where console.* API is implemented

* backward compatible with console.*
  * so you can use do "console = logjs;" in your code to use logjs immediatly
  

